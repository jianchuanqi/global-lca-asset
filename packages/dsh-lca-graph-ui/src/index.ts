/** Host loader entry for the browser-only Global LCA graph presentation plugin. */

/** The host half is intentionally empty; all behavior runs in the DSH browser client. */
export function apply(): void {}
