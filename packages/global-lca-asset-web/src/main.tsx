import React from 'react';
import ReactDOM from 'react-dom/client';
import GlobalLcaAsset from './GlobalLcaAsset';
import './styles.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <GlobalLcaAsset />
  </React.StrictMode>,
);
